# Task 17+ — Phase 3 preview (request full prompts when Phase 2 ships)

Remaining epics, in suggested order:
- G1: exoplanet transit-detection pipeline (lightkurve + PyTorch) run
  entirely on-platform — the platform's own acceptance test and the
  flagship ML demo. Software-only; can start any time after B4.
- A4: TOTP 2FA for admin accounts.
- D1–D3: Incus compute instances — REQUIRES Track B (real Ubuntu host).
- F2: Prometheus + Grafana + nvidia-smi exporter, alert rules.
- Cloudflare Tunnel: deliberate public exposure of selected endpoints.
- Mini-PC migration: control plane moves off the laptop (docker compose
  transplant + Tailscale rejoin), TUF becomes pure GPU node.

Do not start these from this stub — request full prompts first.
